class Foo {
  public bar(a: number, b: number): number {
    // 20 lines of code
  }

  public baz(c: number, d: number): number {
    // 10 lines of code
  }

  public qux(e: number, f: number): number {
    // 10 lines of code
  }

  public quux(g: number, h: number): number {
    // 10 lines of code
  }
}
